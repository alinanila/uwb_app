diff --git a/config/uwb_agent.yaml b/config/uwb_agent.yaml
index c8c07aa..f7366f5 100644
--- a/config/uwb_agent.yaml
+++ b/config/uwb_agent.yaml
@@ -16,8 +16,8 @@ fira:
   # multi_node_mode: onetomany enables O2M for multiple anchors.
   multi_node_mode: onetomany
   slot_duration: 2400
-  ranging_interval: 200
-  slots_per_rr: 25
+  ranging_interval: 30
+  slots_per_rr: 12
   hopping_mode: disabled
   aoa_report: all-enabled
   max_measurements: 0
@@ -43,19 +43,20 @@ tag:
   connect: false
   port: "COM18"
 
+# comment out anchors as appropriate
 anchors:
   - id: "A"
-    port: "COM16"
+    port: "/dev/ttyACM0"
     mac: 0x0001
-  - id: "B"
-    port: "COM17"
-    mac: 0x0002
-  - id: "C"
-    port: "COM11"
-    mac: 0x0003
-  - id: "D"
-    port: "COM19"
-    mac: 0x0004
+  # - id: "B"
+  #   port: "/dev/ttyACM0"
+  #   mac: 0x0002
+  # - id: "C"
+  #   port: "/dev/ttyACM0"
+  #   mac: 0x0003
+  # - id: "D"
+  #   port: "/dev/ttyACM0"
+  #   mac: 0x0004
 
 # Optional dedup for listen=both. Applied per device to avoid dropping
 # legitimate measurements from multiple anchors.
diff --git a/config/uwb_hub.yaml b/config/uwb_hub.yaml
index 9b7dd3e..778074c 100644
--- a/config/uwb_hub.yaml
+++ b/config/uwb_hub.yaml
@@ -1,10 +1,10 @@
 hub:
-  enabled: false
+  enabled: true
   upstream_endpoints:
     # - "tcp://127.0.0.1:5556" # use for single-host mode
-    - "tcp://192.168.1.101:5556" # anchor A agent PUB
-    - "tcp://192.168.1.102:5556" # anchor B agent PUB
-    - "tcp://192.168.1.103:5556" # anchor C agent PUB
+    - "tcp://192.168.137.219:5556" # anchor A agent PUB
+    - "tcp://192.168.137.176:5556" # anchor B agent PUB
+    - "tcp://192.168.137.185:5556" # anchor C agent PUB
     - "tcp://127.0.0.1:5556"     # anchor D agent PUB (local)
   upstream_topic: "meas"
   downstream_endpoint: "tcp://127.0.0.1:5560"
diff --git a/config/uwb_localizer.yaml b/config/uwb_localizer.yaml
index ddfa5bd..95395b5 100644
--- a/config/uwb_localizer.yaml
+++ b/config/uwb_localizer.yaml
@@ -1,5 +1,5 @@
 localizer:
-  enabled: false
+  enabled: true
   subscribe_endpoint: "tcp://127.0.0.1:5560"
   subscribe_topic: "meas"
   rcvhwm: 128
@@ -10,8 +10,8 @@ localizer:
   total_anchors: 4
   console: true
   pose_zmq:
-    enabled: false
-    endpoint: "tcp://127.0.0.1:5561"
+    enabled: true
+    endpoint: "tcp://0.0.0.0:5561"
     bind: true
     topic: "pose"
     sndhwm: 32
diff --git a/src/uwb_app/localize.py b/src/uwb_app/localize.py
index b32748f..6ddf2d2 100644
--- a/src/uwb_app/localize.py
+++ b/src/uwb_app/localize.py
@@ -140,6 +140,7 @@ class Localizer:
         self._dropped_incomplete = 0
         self._dropped_bad = 0
         self._publisher = PosePublisher(cfg) if cfg.pose_sink.enabled else None
+        self._peer_sessions: dict[str, set[int]] = {}
 
     def _process_message(self, payload: bytes, now_mono: float) -> None:
         try:
@@ -195,6 +196,23 @@ class Localizer:
         if self.cfg.total_anchors is not None and len(state.measurements) >= self.cfg.total_anchors:
             self._emit_round(key, state, event)
 
+        # log issue if session IDs unsync again
+        sessions_by_peer = getattr(self, "_peer_sessions", None)
+        if sessions_by_peer is None:
+            sessions_by_peer = {}
+            self._peer_sessions = sessions_by_peer
+
+        sessions = sessions_by_peer.setdefault(peer_label, set())
+        sessions.add(session_handle)
+
+        if len(sessions) > 1:
+            log.warning(
+                "Multiple session handles for peer %s: %s; "
+                "this may prevent anchors from fusing into one round",
+                peer_label, 
+                sorted(sessions),
+            )
+
     def _emit_round(
         self,
         key: tuple[int, str],
diff --git a/systemd/install_services.sh b/systemd/install_services.sh
new file mode 100644
index 0000000..4139416
--- /dev/null
+++ b/systemd/install_services.sh
@@ -0,0 +1,59 @@
+#!/usr/bin/env bash
+set -euo pipefail
+
+# Usage:
+#./systemd/install_services.sh agent
+#./systemd/install_services.sh hub
+#./systemd/install_services.sh localizer
+#./systemd/install_services.sh all
+
+SERVICE_DIR="/etc/systemd/system"
+REPO_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
+
+install_agent() {
+  echo "Installing uwb-agent.service..."
+  sudo cp "$REPO_DIR/systemd/uwb-agent.service" "$SERVICE_DIR/"
+  sudo systemctl daemon-reload
+  sudo systemctl enable uwb-agent
+  sudo systemctl restart uwb-agent
+  sudo systemctl status uwb-agent --no-pager || true
+}
+
+install_hub() {
+  echo "Installing uwb-hub.service..."
+  sudo cp "$REPO_DIR/systemd/uwb-hub.service" "$SERVICE_DIR/"
+  sudo systemctl daemon-reload
+  sudo systemctl enable uwb-hub
+  sudo systemctl restart uwb-hub
+  sudo systemctl status uwb-hub --no-pager || true
+}
+
+install_localizer() {
+  echo "Installing uwb-localize.service..."
+  sudo cp "$REPO_DIR/systemd/uwb-localize.service" "$SERVICE_DIR/"
+  sudo systemctl daemon-reload
+  sudo systemctl enable uwb-localize
+  sudo systemctl restart uwb-localize
+  sudo systemctl status uwb-localize --no-pager || true
+}
+
+case "${1:-}" in
+  agent)
+    install_agent
+    ;;
+  hub)
+    install_hub
+    ;;
+  localizer)
+    install_localizer
+    ;;
+  all)
+    install_agent
+    install_hub
+    install_localizer
+    ;;
+  *)
+    echo "Usage: $0 {agent|hub|localizer|all}"
+    exit 1
+    ;;
+esac
\ No newline at end of file
diff --git a/systemd/uwb-agent.service b/systemd/uwb-agent.service
new file mode 100644
index 0000000..d52da53
--- /dev/null
+++ b/systemd/uwb-agent.service
@@ -0,0 +1,13 @@
+[Unit]
+Description=UWB Agent
+After=network.target
+
+[Service]
+User=localuwb
+WorkingDirectory=/home/localuwb/uwb_app
+ExecStart=/home/localuwb/.local/bin/hatch run uwb-run --config config/uwb_agent.yaml -t -1
+Restart=always
+RestartSec=2
+
+[Install]
+WantedBy=multi-user.target
\ No newline at end of file
diff --git a/systemd/uwb-hub.service b/systemd/uwb-hub.service
new file mode 100644
index 0000000..789cd74
--- /dev/null
+++ b/systemd/uwb-hub.service
@@ -0,0 +1,14 @@
+[Unit]
+Description=UWB Hub
+After=network.target
+Requires=uwb-agent.service
+
+[Service]
+User=localuwb
+WorkingDirectory=/home/localuwb/uwb_app
+ExecStart=/home/localuwb/.local/bin/hatch run uwb-hub --config config/uwb_hub.yaml
+Restart=always
+RestartSec=2
+
+[Install]
+WantedBy=multi-user.target
\ No newline at end of file
diff --git a/systemd/uwb-localize.service b/systemd/uwb-localize.service
new file mode 100644
index 0000000..71d735e
--- /dev/null
+++ b/systemd/uwb-localize.service
@@ -0,0 +1,14 @@
+[Unit]
+Description=UWB Localizer
+After=network.target uwb-hub.service
+Requires=uwb-hub.service
+
+[Service]
+User=localuwb
+WorkingDirectory=/home/localuwb/uwb_app
+ExecStart=/home/localuwb/.local/bin/hatch run uwb-localize --config config/uwb_localizer.yaml
+Restart=always
+RestartSec=2
+
+[Install]
+WantedBy=multi-user.target
